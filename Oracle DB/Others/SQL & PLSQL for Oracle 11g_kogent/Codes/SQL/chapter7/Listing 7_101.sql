SELECT D1.CNAME FROM DEPOSIT D1, CUSTOMER C1 WHERE D1.CNAME=C1.CNAME AND C1.CITY IN (SELECT C2.CITY FROM CUSTOMER C2 GROUP BY C2.CITY HAVING COUNT(C2.CITY)=( SELECT MAX(COUNT(*)) FROM CUSTOMER GROUP BY CITY));