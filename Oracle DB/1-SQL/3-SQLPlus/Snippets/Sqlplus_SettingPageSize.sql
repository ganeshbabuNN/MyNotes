SET PAGESIZE 1000
Select * from emp;

--the above example, the page size is set to 10 lines and then a query is run
--Notice that the headings are shown only once, at the top, and the resulting ouput looks better
