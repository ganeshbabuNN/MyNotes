# While Statement
# It executes a statements repetitively until the conditions is no longer true
#statement in samel line sep with ;
 i <- 10
 while(i > 0){print("hello");i <- i-1}
 
#without ;
 i <- 10
 while(i > 0)
 {
   print("hello")
   i <- i-1
  }