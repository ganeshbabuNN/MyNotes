find_amount ( );          		--Accessing own object 
u1. find_amount ( );   		--Accessing object of user ul
find_amount@remote network 
					--Accessing object or remote mode named as 							--Remote network. 
					--The data link name is remote_network.
ul.find_object@remote_network 
					--Qualified name of remote node object.
